services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    environment:
      - ADMIN_PASSWORD=admin123
      - DB_PATH=/app/data/parts.db
    restart: unless-stopped
